import { ThemeProps } from '@/theme/@types';

export const hr: ThemeProps = {
  components: {
    Divider: {
      baseStyle: {
        borderWidth: '1px',
        borderColor: 'gray.100'
      }
    }
  }
};
